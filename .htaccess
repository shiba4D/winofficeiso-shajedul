##############################
# 1) Set Default Index Order #
##############################
# Make sure index.html is served before index.php, if both exist.
DirectoryIndex index.html index.php

########################################################
# 2) PHP 8.1 HANDLER (if your files need PHP at all)  #
########################################################
<IfModule mime_module>
  AddHandler application/x-httpd-ea-php81 .php .php8 .phtml
</IfModule>

########################################################
# 3) (OPTIONAL) DISABLE ALL BROWSER/PROXY CACHING      #
#    Remove or comment out if you do want caching.     #
########################################################
<IfModule mod_headers.c>
  Header set Cache-Control "no-cache, no-store, must-revalidate"
  Header set Pragma "no-cache"
  Header set Expires "0"
</IfModule>

########################################################
# 4) NO CATCH-ALL REWRITE RULES                        #
#    This ensures any existing .html or .php file is   #
#    served directly—nothing gets forced to index.php. #
########################################################

# (If you ever need custom redirects later, you can add them here.)
# Example (commented out):
# <IfModule mod_rewrite.c>
#   RewriteEngine On
#   # Redirect non-www to www:
#   # RewriteCond %{HTTP_HOST} !^www\. [NC]
#   # RewriteRule ^(.*)$ https://www.yourdomain.com/$1 [L,R=301]
# </IfModule>

# php -- BEGIN cPanel-generated handler, do not edit
# Set the “ea-php81” package as the default “PHP” programming language.
<IfModule mime_module>
  AddHandler application/x-httpd-ea-php81 .php .php8 .phtml
</IfModule>
# php -- END cPanel-generated handler, do not edit
